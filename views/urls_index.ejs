<!DOCTYPE html>

<html lang="en">

  <head>
    <style>
      table {
        border-style: hidden;
        border-radius: 14px;
        padding: 20px;
        text-align: left;
        font-size: 20 px;
        background-color: lightblue;
      }
      tr:hover{background-color:lightskyblue}
      button {
        background-color: whitesmoke;
        border-radius: 20px;
        cursor: pointer;
        font-size: 15px;
      }
      .container{
        max-width: 1000px;
        margin: 0 auto;
      }
    </style>
  </head>

  <body>
    <% include partials/_header %>

    <div class="container">
      <table style="width:70%">
        <!-- header for the table one time only -->
        <tr>
          <th>Short URLs</th>
          <th colspan="2">URLs</th>
        </tr>
        <!-- Data part of the Table -->
        <% for(var k in templateVars) { %>
          <tr>
            <td><%= k %></td>
            <td><%= templateVars[k] %></td>
            <td><form method="POST" action="/urls/<%= k %>/delete"><button type= "submit"> Delete URL</button></form></td>
            <td><a href="/urls/<%= k %>"><button type= "submit">Edit URL</a></td>
          </tr>
        <% } %>
      </table> <!-- Table tag closes -->
      <a href="urls/new"><button type= "submit">Let's create a Tiny Link!</button></a>
    </div>
    

  </body>

</html>